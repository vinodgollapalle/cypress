{
  "name": "cypress",
  "version": "1.0.0",
  "main": "index.js",
  "description": "",
  "author": "",
  "license": "ISC",
  "scripts": {
  "cypress:run": "cypress run",
  "merge-reports": "node merge-reports.js",
  "generate-report": "npx marge reports/merged-report.json --reportDir reports --reportFilename index.html",
  "test": "npm run cypress:run && npm run merge-reports && npm run generate-report"
},
  
  "dependencies": {
    "cypress": "^14.5.0"
  },
  "devDependencies": {
    "@4tw/cypress-drag-drop": "^2.3.0",
    "cypress-mochawesome-reporter": "^4.0.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
