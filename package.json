{
  "name": "cypress",
  "version": "1.0.0",
  "main": "index.js",
  "description": "",
  "author": "",
  "license": "ISC",
  "scripts": {
    "test": "cypress run",
    "merge-reports": "mochawesome-merge reports/mochawesome/*.json > reports/mochawesome/merged-report.json",
    "generate-report": "marge reports/mochawesome/merged-report.json --reportDir reports/mochawesome --reportFilename mochawesome-report",
    "test:report": "npm run test && npm run merge-reports && npm run generate-report"
  },
  "dependencies": {
    "cypress": "^14.5.0"
  },
  "devDependencies": {
    "@4tw/cypress-drag-drop": "^2.3.0",
    "cypress-mochawesome-reporter": "^4.0.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
